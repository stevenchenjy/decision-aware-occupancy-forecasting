# Archived NEW figure trees

The `figure/` and `figures/` subdirectories preserve the two competing staged
figure locations exactly as found. They are provenance only. Active,
regenerable figures live in the repository-root `figures/` directory.
