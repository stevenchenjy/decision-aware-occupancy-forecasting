# Python 3.10-3.12 is recommended. Python 3.11 was used for review-readiness CI.
pandas>=2.2,<3.0
numpy>=1.26,<2.0
scikit-learn>=1.4,<2.0
lightgbm>=4.0,<5.0
# macOS note: if LightGBM cannot load libomp.dylib, install OpenMP first
# with `brew install libomp`, or use the Conda environment in environment.yml.
# Avoid a local-version pin such as torch==...+cpu; that requires a custom
# package index and breaks plain `pip install -r requirements.txt`.
torch>=2.2,<3.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<0.14
ipykernel>=6.29
openpyxl>=3.1
pytest>=8.0
